```
# 推出非终结符
函数定义          -> 修饰词闭包 类型 变量 ( <参数声明> ) { 函数块 }
eg:  const static|int * func_name ( ) {  }

修饰词闭包        -> 修饰词 修饰词闭包
eg:  extern const static register

修饰词闭包        -> @

取地址            -> 星号闭包
星号闭包          -> 星号 星号闭包
星号闭包          -> @
eg:  *****

变量             -> 标识符 数组下标
eg: array[10+100*3]
因式             -> ( 表达式 ) 
因式             -> 变量
因式             -> 数字  

后缀表达式        -> 变量 后缀运算符
eg: a++, i--

声明             -> 修饰词闭包 类型 变量 赋初值  

函数块           -> 声明语句闭包 函数块闭包

函数块闭包  -> 赋值函数 函数块闭包
函数块闭包  -> for循环 函数块闭包
函数块闭包  -> 条件语句 函数块闭包
函数块闭包  -> 函数返回 函数块闭包
函数块闭包  -> @  

赋值函数    -> 变量 赋值或函数调用
```



**混合- 循环，条件，分支，返回，结构/函数定义**

```
for循环         -> for ( 赋值函数 逻辑表达式 ; 后缀表达式 ) { 函数块 }  
for ()

赋值或函数调用   -> = <右值> ; 
赋值或函数调用   -> ( <参数列表> ) ; 

条件语句        -> if ( 逻辑表达式 ) { 函数块 } 否则语句  

逻辑表达式      -> 表达式 逻辑运算符 表达式 
否则语句        -> else { 函数块 }



函数返回       -> return 因式  ;

后缀表达式     -> 变量 后缀运算符
```



**推出终结符：常量/运算符/关键字/标识符/修饰词**

```
数字         -> const_digit
标识符       -> id
修饰词       -> describe

逻辑运算符    -> <
逻辑运算符    -> >
逻辑运算符    -> ==
逻辑运算符    -> !=

后缀运算符    -> ++
后缀运算符    -> --
```

